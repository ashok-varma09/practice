
#include <stdio.h>

int main()
{
    int num,fd,ld,sum;
    printf("Enter any integer numbers :");
    scanf("%d",&num);
    ld=num;
    while (ld>=10)
    {
        ld=ld/10;
    }
     
    fd=num%10;
    sum=fd+ld;
    printf("%d",sum);

    return 0;
}
